<h1>Listing home_configurations</h1>

<table>
  <tr>
    <th>Main history</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @home_configurations.each do |home_configuration| %>
  <tr>
    <td><%= home_configuration.main_history %></td>
    <td><%= link_to 'Show', home_configuration %></td>
    <td><%= link_to 'Edit', edit_home_configuration_path(home_configuration) %></td>
    <td><%= link_to 'Destroy', home_configuration, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Home configuration', new_home_configuration_path %>
